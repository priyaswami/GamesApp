import Config from "react-native-config";

const { ENVIRONMENT, BASE } = Config;

const DEV = 'dev'
const DEV_URL = '' //url for dev

// Authentication 

// games endpoints

export const TRENDING_GAMES = BASE + ""